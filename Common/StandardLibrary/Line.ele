struct Line(direction:Vector3, offset:Vector3)
{
    distanceFromPoint(line:Line, point:Vector3):num
    {
        fromPointToPointOnLine = point.sub(line.offset);
        return = fromPointToPointOnLine.dot(line.direction);
    }
}