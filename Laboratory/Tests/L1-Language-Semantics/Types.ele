TestImplicitConversion(a:num):num
{
    TestAlias:num;
    alias = TestAlias(a);
    AddAliased(n:num):TestAlias = add(n, 5);
    return = AddAliased(alias);
}

Voldemort(a)
{
	return struct CannotBeNamed(a);
}