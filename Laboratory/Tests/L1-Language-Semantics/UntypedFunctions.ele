intrinsic add(a, b);
intrinsic sub(a, b);

addThree(a, b, c) = add(a, add(b, c));

subBTwice(a, b)
{
	heheIshadow(a, b) = add(a, b);
    first = sub(a, b);
    return = sub(first, b);
}

a = add(5, 8);              # 13
b = sub(3, 5.5);            # -2.5
c = add(a, b);              # 10.5
d = sub(add(c, b), 2);      # 6
e = addThree(c, d, 1.5);    # 18
f = subBTwice(e, b);        # 23